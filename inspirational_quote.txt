created a git directory using mkdir
created a file using touch filename
added steps in the file
---
now run git init to initalize git
we can use ls -a to see all the files includding hidden files

add new changes

%%%%%%%%%% if miss to add message -m then follow below steps %%%%%%%%%%%
press i (to insert)
then type the message and press escape
then type :wq
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
==== following are the git commands

git init
git status
git add
git commit
git log
